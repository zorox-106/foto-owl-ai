/**
 * Remotion entry point.
 * The EventReel.tsx composition is written here by the ScriptGenerator agent.
 * This stub root is replaced when the pipeline runs.
 */
import { registerRoot } from 'remotion';
import { RemotionRoot } from './Root';

registerRoot(RemotionRoot);
