/**
 * EventReel.tsx — PLACEHOLDER
 *
 * This file is generated and overwritten by the FotoOwl pipeline.
 * It is committed as a placeholder to satisfy TypeScript imports
 * before the first pipeline run.
 */
import React from 'react';
import { AbsoluteFill } from 'remotion';

export const EventReel: React.FC = () => {
  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#000',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <div
        style={{
          color: '#fff',
          fontSize: 48,
          fontFamily: 'sans-serif',
          textAlign: 'center',
        }}
      >
        Run the FotoOwl pipeline to generate your event reel
      </div>
    </AbsoluteFill>
  );
};

export default EventReel;
